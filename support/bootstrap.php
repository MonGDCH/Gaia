<?php


/*
|------------------------------------------------------------------------------------
| 错误信息处理
|------------------------------------------------------------------------------------
| 这里定义PHP错误处理，开启所有错误信息
|
*/
ini_set('display_errors', 'on');
error_reporting(E_ALL);


/*
|------------------------------------------------------------------------------------
| 定义应用根路径
|------------------------------------------------------------------------------------
| 这里定义根路径, 用于路径查找，在phar环境下，根路径为phar文件
|
*/
define('BASE_PATH', defined('IN_PHAR') ? Phar::running() : dirname(__DIR__));


/*
|------------------------------------------------------------------------------------
| 定义应用根路径
|------------------------------------------------------------------------------------
| 这里定义根路径, 用于路径查找，在phar环境下，根路径为phar文件所在目录
|
*/
define('ROOT_PATH', defined('IN_PHAR') ? dirname(Phar::running(false)) : dirname(__DIR__));


/*
|------------------------------------------------------------------------------------
| 定义应用APP路径
|------------------------------------------------------------------------------------
| 这里定义用户主要编写业务代码存放路径, 用于路径查找
|
*/
define('APP_PATH', BASE_PATH . '/app');


/*
|------------------------------------------------------------------------------------
| 扩展支持库路径
|------------------------------------------------------------------------------------
| 这里定义系统及插件相关扩展支持库存放路径
|
*/
define('SUPPORT_PATH', BASE_PATH . '/support');


/*
|------------------------------------------------------------------------------------
| 定义配置文件路径
|------------------------------------------------------------------------------------
| 这里定义配置文件路径, 用于路径查找获取配置信息
|
*/
define('CONFIG_PATH', BASE_PATH . '/config');


/*
|------------------------------------------------------------------------------------
| 自定义进程驱动类目录路径
|------------------------------------------------------------------------------------
| 这里定义自定义的进程驱动类目录，目录最好拥有读写权限
|
*/
define('PROCESS_PATH', APP_PATH . '/process');


/*
|------------------------------------------------------------------------------------
| 自定义指令类目录路径
|------------------------------------------------------------------------------------
| 这里定义自定义的指令类目录，目录最好拥有读写权限
|
*/
define('COMMAND_PATH', APP_PATH . '/command');


/*
|------------------------------------------------------------------------------------
| 定义配置文件路径
|------------------------------------------------------------------------------------
| 这里定义配置文件路径, 用于路径查找获取配置信息
|
*/
define('ENV_PATH', ROOT_PATH . '/.env');


/*
|------------------------------------------------------------------------------------
| 进程启动入口文件路径
|------------------------------------------------------------------------------------
| 这里定义进程驱动启动入口文件的目录，入口文件基于workerman管理进程
|
*/
define('BIN_PATH', ROOT_PATH . '/bin');


/*
|------------------------------------------------------------------------------------
| 运行时目录路径
|------------------------------------------------------------------------------------
| 这里定义运行时路径, 用于存储系统运行时相关资源内容，目录需拥有读写权限
| 如果打包为phar运行，则需要将路径写为固定绝对路径，如：/home/www/gaia/runtime
|
*/
define('RUNTIME_PATH', ROOT_PATH . '/runtime');


/*
|------------------------------------------------------------------------------------
| 定义外部可访问资源目录路径
|------------------------------------------------------------------------------------
| 这里定义外部可访问目录路径，目录最好拥有读写权限
|
*/
define('PUBLIC_PATH', ROOT_PATH . '/public');


/*
|------------------------------------------------------------------------------------
| 定义应用启动模式
|------------------------------------------------------------------------------------
| 这里定义windows下应用启动模式, 处理在部分windows环境下start服务自动重启的问题
|
*/
define('COMPATIBLE_MOD', false);
